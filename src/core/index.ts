/**
 * @namespace PIXI
 */

namespace gobi.core {
    // export function autoDetectRenderer(width = 800, height = 600, options?: IRendererOptions, noWebGL?: boolean) {
    //     if (!noWebGL && utils.isWebGLSupported()) {
    //         return new WebGLRenderer(width, height, options);
    //     }

        // return new CanvasRenderer(width, height, options);
    // }
}
